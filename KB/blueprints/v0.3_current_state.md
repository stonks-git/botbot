# Blueprint v0.3: Current State — Unified Memory Architecture

> **Current version.** Supersedes v0.1 (original 9-phase plan) and v0.2 (rework plan for D-005).
> v0.1 and v0.2 are historical — do not load unless tracing decision evolution.

## What BotBot Is

BotBot bolts the intuitive-AI cognitive architecture onto OpenClaw as a Python sidecar brain service, giving OpenClaw agents a real mind that develops identity from experience.

**Stack**: Python 3.12 (brain) + Node.js/TypeScript (OpenClaw) + PostgreSQL 17 + pgvector

## Architecture

```
┌─────────────────────────────────────────────────┐
│  Docker Compose                                 │
│                                                 │
│  ┌──────────────┐   HTTP    ┌────────────────┐  │
│  │   OpenClaw   │◄────────►│  Brain Service  │  │
│  │  (Node.js)   │  :8400   │   (Python)      │  │
│  │              │          │                  │  │
│  │ memory-brain │          │ Memory + Gates   │  │
│  │   plugin     │          │ Context Assembly │  │
│  │              │          │ Gut Feeling      │  │
│  │              │          │ Consolidation    │  │
│  │              │          │   (background)   │  │
│  │              │          │ DMN / Idle Loop  │  │
│  │              │          │   (background)   │  │
│  │              │          │ Safety Monitor   │  │
│  └──────────────┘          └───────┬──────────┘  │
│                                    │             │
│                            ┌───────▼──────────┐  │
│                            │   PostgreSQL 17  │  │
│                            │   + pgvector     │  │
│                            └──────────────────┘  │
└─────────────────────────────────────────────────┘
```

## Core Design Principle: Identity IS the Weights (D-005)

There are no L0/L1 JSON files. No `layers.py`. Identity emerges from the unified `memories` table:

- Memory reinforced → high alpha → high weight center → surfaces more in stochastic sampling
- Memory contradicted → high beta → low weight center → surfaces less
- Identity = the top-N highest-weight memories. Values, beliefs, goals are just memories that survived reinforcement.
- Agent core metadata (name, persona) = immutable seed memories with `Beta(8,2)` and `immutable=true`

## What's Done (Phases 0-3)

### Phase 0: Foundation — DONE
- `brain/Dockerfile`, `brain/requirements.txt`, `brain/pyproject.toml`
- `brain/src/schema.sql` — 5 tables: memories (halfvec(3072), Beta weights, immutable flag), scratch_buffer, memory_co_access, memory_supersedes, consolidation_log
- `brain/src/db.py` — asyncpg pool + schema migration
- `brain/src/api.py` — FastAPI on :8400
- `docker-compose.yml` — postgres (pgvector/pgvector:pg17), brain, openclaw

### Phase 1: Memory Core — DONE
- `brain/src/config.py` — RetryConfig, EMBED_MODEL (`gemini-embedding-001`), EMBED_DIMENSIONS (3072), MEMORY_TYPE_PREFIXES (8 types)
- `brain/src/stochastic.py` — StochasticWeight: Beta(alpha,beta) with observe/reinforce/contradict
- `brain/src/activation.py` — ACT-R: B+S+P+epsilon, cosine_similarity, spreading_activation
- `brain/src/relevance.py` — 5-component Dirichlet relevance, co-access (Hebbian), spread_activation
- `brain/src/memory.py` — MemoryStore: embed (Gemini), store, search_similar/hybrid/reranked, retrieval mutation, store_insight, why_do_i_believe

**API**: POST /memory/store, POST /memory/retrieve, GET /memory/{id}, DELETE /memory/{id}

**Plugin** (`openclaw/extensions/memory-brain/`):
- Tools: memory_recall, memory_store, memory_forget
- Hook `before_agent_start`: context assembly → inject via prependContext
- Hook `agent_end`: auto-capture user messages via gate (max 3/session)

### Phase 2: Entry/Exit Gate — DONE
- `brain/src/gate.py` — EntryGate (stochastic filter) + ExitGate (3x3 decision matrix)
- Entry: short (<10 chars) 95% skip, mechanical (prefixes) 90% skip, normal 99% buffer
- Exit: relevance (core/peripheral/irrelevant) × novelty (confirming/novel/contradicting) → persist/reinforce/buffer/drop
- Score modulation: `base_score * (0.5 + 0.5 * s_i) + emotional_charge_bonus`
- Noise floor: 2% chance DROP → BUFFER

**API**: POST /memory/gate

### Phase 3: Context Assembly + Unified Identity — DONE (D-005 rework)
- `brain/src/context_assembly.py` — 3-track context assembly:
  - Track 0: immutable safety memories (always present)
  - Identity hash: compact ~100-200 tokens from top-10 memories by weight center
  - Track 2: stochastic identity memories (Beta-sampled `observe() > 0.6` from top-20 by weight)
  - Track 1: situational memories (hybrid search within budget)
- `render_identity_hash(memory_store, agent_id)` — from DB, replaces LayerStore
- `render_identity_full(memory_store, agent_id)` — from DB, grouped by memory type
- `render_system_prompt()` → `[SAFETY BOUNDARIES]`, `[IDENTITY]`, `[IDENTITY -- active beliefs/values]`, `[RELEVANT MEMORIES]`
- Gate wiring: `_get_identity_embeddings()` in api.py queries top-20 memories by weight center, `embedding::float4[]` SQL cast for asyncpg deserialization → `np.array()`

**API**: POST /context/assemble, GET /identity/{agent_id}, GET /identity/{agent_id}/hash
**Plugin**: introspect tool (view identity, full or hash mode)

## Current Dependency Graph

```
config.py            <- standalone
stochastic.py        <- standalone
activation.py        <- numpy
relevance.py         <- activation
memory.py            <- config, relevance, db (pool)
gate.py              <- activation, memory
gut.py               <- numpy, json, pathlib
context_assembly.py  <- stochastic, activation (cosine_similarity)
llm.py               <- config (RetryConfig), anthropic
consolidation.py     <- llm, memory, activation (cosine_similarity), numpy, asyncpg (direct pool for logging)
api.py               <- memory, gate, context_assembly, gut, consolidation, db, numpy
```

## Current API Surface

```
GET  /health
POST /memory/store           { agent_id, content, memory_type, source?, tags, confidence, importance, metadata?, source_tag? }
POST /memory/retrieve        { agent_id, query, top_k, mode }
GET  /memory/{id}            ?agent_id=X
DELETE /memory/{id}          ?agent_id=X
POST /memory/gate            { agent_id, content, source?, source_tag }
POST /context/assemble       { agent_id, query_text, conversation, total_budget }
GET  /identity/{agent_id}
GET  /identity/{agent_id}/hash
POST /context/attention      { agent_id, embedding }
GET  /gut/{agent_id}
GET  /consolidation/status
POST /consolidation/trigger  { agent_id }
```

## What's Done (Phase 4)

### Phase 4: Gut Feeling — DONE
- `brain/src/gut.py` — GutFeeling (EMA attention, DB-weighted subconscious, GutDelta, persistence)
- `POST /context/attention` — standalone attention update endpoint
- `GET /gut/{agent_id}` — current gut state endpoint
- Gate wired: `attention_embedding=gut.attention_centroid`, `emotional_charge=gut.emotional_charge`
- Context assembly wired: attention centroids for inertia + gut summary as `[COGNITIVE STATE]`
- Plugin: `gut_check` tool + `brainGetGutState()` / `brainUpdateAttention()` clients
- D-005 adaptation: subconscious centroid from top-N identity memory embeddings weighted by Beta center
- api.py version 0.3.0

## What's Done (Phase 5)

### Phase 5: Consolidation Engine — DONE
- `brain/src/llm.py` — Anthropic Claude wrapper (claude-haiku-4-5, retry)
- `brain/src/consolidation.py` — ConstantConsolidation (Tier 1: decay_tick/contradiction_scan/pattern_detection) + DeepConsolidation (Tier 2: merge_and_insight/promote_patterns/decay_and_reconsolidate/tune_parameters/contextual_retrieval) + ConsolidationEngine wrapper
- `brain/src/api.py` — background task in lifespan, GET /consolidation/status, POST /consolidation/trigger, version 0.4.0
- Plugin: consolidation_status + consolidation_trigger tools
- D-005 simplification: promote_patterns does direct SQL alpha updates, no L0/L1 writes
- Error isolation per operation, multi-agent support, 9 consolidation_log writes, 2 store_insight calls

## What's Next (Phases 6-8)

### Phase 6: DMN / Idle Loop — NEXT
**Goal**: Spontaneous self-prompts during agent downtime ("shower thoughts")

Port: `idle.py`, `rumination.py`
Create: `brain/src/dmn_store.py` — in-memory thought queue per agent_id
Add endpoint: `GET /dmn/thoughts`
Update plugin: background poll every 30s, queue as self-prompts when idle

4 sampling channels: neglected (35%), tension (20%), temporal (20%), introspective (25%)

**Verify**: Leave agent idle 10+ min → thoughts generated → diverse channels → rumination threads form

---

### Phase 7: Safety Monitor — AFTER Phase 5
**Goal**: Guardrails on memory weight evolution

Port: `safety.py` (ceiling/limiter core, not full 21K lines)
Wire into: `MemoryStore.safety` reference
Phases: A (hard ceiling 0.95, diminishing returns), B (rate limiter), C (entropy monitor, circuit breaker)

**Verify**: Repeated reinforcement hits diminishing returns → ceiling → audit log

---

### Phase 8: Bootstrap Readiness — AFTER Phase 5+6
**Goal**: 10-milestone maturation tracking for new agents

Port: `bootstrap.py`
Add endpoint: `GET /bootstrap/status`

**D-005 simplification**: All milestone checks query DB directly (weight centers, memory counts). No L0/L1 files to check.

**Verify**: New agent starts 0/10 → milestones unlock progressively

## Phase Dependency Graph

```
Phase 0 -- Phase 1 (MVP) -- Phase 2 (Gates) -- Phase 3 (Context + Identity)
                                                         |
                                                         +-- Phase 4 (Gut) DONE
                                                         |
                                                         +-- Phase 5 (Consolidation) DONE
                                                         |         +-- Phase 7 (Safety)
                                                         |         +-- Phase 8 (Bootstrap)
                                                         |
                                                         +-- Phase 6 (DMN) <-- YOU ARE HERE
```

## Key Design Decisions

| ID | Decision | Status |
|----|----------|--------|
| D-001 | Python sidecar brain + TypeScript plugin, PostgreSQL+pgvector | accepted |
| D-002 | Embedding: gemini-embedding-001 at 3072 dimensions (max Matryoshka) | accepted |
| D-003 | Full brain scope: memory + gut + consolidation + DMN + safety + bootstrap | accepted |
| D-004 | PostgreSQL 17 + pgvector for all brain storage (replaces LanceDB) | accepted |
| D-005 | Unified memory: discard L0/L1 layers, identity from Beta weights in unified table | accepted |

## Patterns Established

- **asyncpg halfvec deserialization**: Use `embedding::float4[]` SQL cast → returns Python list → `np.array(..., dtype=np.float32)`
- **Agent namespacing**: Every DB query includes `agent_id` filter. Every endpoint requires it.
- **Stochastic injection**: Use `StochasticWeight.observe() > threshold` for probabilistic context inclusion
- **Graceful degradation**: If brain unreachable, plugin logs warning and falls back (raw memory retrieval or no enrichment)
- **Weight center query**: `depth_weight_alpha / (depth_weight_alpha + depth_weight_beta)` — indexed in schema
