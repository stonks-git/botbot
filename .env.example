# Required: Google API key for Gemini (used by both brain embeddings and agent conversations)
GOOGLE_API_KEY=your-google-api-key-here

# Optional: Gateway access token (defaults to botbot-dev)
OPENCLAW_GATEWAY_TOKEN=botbot-dev

# Optional: Database password (defaults to agent_secret)
AGENT_DB_PASSWORD=agent_secret
