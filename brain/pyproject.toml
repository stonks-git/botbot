[project]
name = "botbot-brain"
version = "0.1.0"
description = "Cognitive architecture sidecar for OpenClaw"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
